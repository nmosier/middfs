MIDDFS INSTALLATION INSTRUCTIONS
09/27/2019

BUILDING
cd into `src'
run
> make
The result of the build is a *.ko file.

INSTALLING
run
> sudo make load
or, alternatively,
> sudo kldload ./middfs.ko

MOUNTING
create the desired mountpoint directory (e.g. `./mnt')
enter in your `/etc/fstab' file the following line:
> middfs ./mnt middfs ro 0 0
then run the command
> sudo mount -t middfs middfs ./mnt

UNMOUNTIJNG
> sudo unomunt ./mnt

REMOVING
run
> sudo make unlod
or, alternatively,
> sudo kldunload ./middfs.ko
